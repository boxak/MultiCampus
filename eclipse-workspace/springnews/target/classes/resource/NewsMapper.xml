<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="resource.NewsMapper">
  	<select id="selectNews" resultType="vo.NewsVO">
  		select id, writer, title, content, to_char(writedate,'yyyy"년"
  		 mm"월" dd"일" hh24"시" mi"분"') writedate, cnt from news
  	</select>
  	<select id="listOneNews" resultType="vo.NewsVO">
  		select id, writer, title, content, to_char(writedate,'yyyy"년"
  		 mm"월" dd"일" hh24"시" mi"분"') writedate, cnt from news 
  		 where id=#{id}
  	</select>
  	<select id="searchNews" parameterType="java.util.HashMap" resultType="vo.NewsVO">
  		select id, writer, title, content, to_char(writedate,'yyyy"년"
  		 mm"월" dd"일" hh24"시" mi"분"') writedate, cnt from news 
  		 where ${searchType} like '%'||#{key}||'%'
  	</select>
  	<select id="listWriterNews" parameterType="string" resultType="vo.NewsVO">
  		select id, writer, title, content, to_char(writedate,'yyyy"년"
  		 mm"월" dd"일" hh24"시" mi"분"') writedate, cnt from news 
  		 where writer like '%'||#{writer}||'%'
  	</select>
  	<insert id="insertNews" parameterType="vo.NewsVO">
  		insert into news (id,writer,title,content,writedate,cnt) values 
  		(#{id},#{writer},#{title},#{content},sysdate,#{cnt})
  	</insert>
  	<delete id="deleteNews" parameterType="_int">
  		delete news where id=#{id}
  	</delete>
  	<update id="updateNews" parameterType="vo.NewsVO">
  		update news set writer=#{writer}, title=#{title}, content=#{content},writedate=sysdate,cnt=#{cnt} where id = #{id}
  	</update>
  	
  </mapper>